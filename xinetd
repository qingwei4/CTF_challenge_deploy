service affine
{
    disable = no
    type = UNLISTED
    socket_type = stream
    protocol = tcp
    server = /home/affine/run.sh
    user = affine
    port = 4240
    flags = REUSE
    bind = 0.0.0.0
    wait = no
}
